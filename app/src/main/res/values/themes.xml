<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TheMovieBookingApp" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/colorPrimaryDark</item>
        <!-- Customize your theme here. -->
        <item name="android:editTextColor">@android:color/white</item>
        <item name="android:textColorHint">#444444</item>
    </style>
    
    <style name="Theme.TheMovieBookingApp.SplashScreenTheme" parent="Theme.TheMovieBookingApp">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:fitsSystemWindows">true</item>
    </style>

    <style name="Theme.TheMovieBookingApp.TransparentStatusBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:fitsSystemWindows">true</item>
        <item name="android:windowBackground">@color/colorPrimary</item>
    </style>

    <style name="myErrorToast">
        <item name="stTextColor">#000</item>
        <item name="stColorBackground">#fff</item>
        <item name="stIconStart">@drawable/ic_baseline_error_outline_24dp</item>
        <item name="stRadius">8dp</item>
        <item name="stLength">LONG</item>
        <item name="stTextSize">16sp</item>
        <item name="stStrokeWidth">1dp</item>
        <item name="stStrokeColor">@color/colorAccent</item>
    </style>
</resources>